# Development tooling — pinned for reproducibility.
# Update with: pip install -U <pkg> && pip freeze | grep <pkg>

ruff==0.6.9
black==26.3.1
mypy==1.13.0
pytest==9.0.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pyyaml==6.0.2
